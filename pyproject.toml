[project]
name = "agent-sandbox-starter"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.1.4",
    "fastapi>=0.119.1",
    "modal>=1.2.1",
]
